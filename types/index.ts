// Add this to your existing types file
export interface Expense {
  id: string;
  item: string;
  amount: number;
  paymentType: string;
  date: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
}
